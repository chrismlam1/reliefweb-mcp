[project]
name = "reliefweb-mcp-server"
version = "0.1.0"
description = "MCP server for ReliefWeb humanitarian information"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0.0",
    "httpx>=0.27.0",
]

[project.scripts]
run-server = "main:main"
